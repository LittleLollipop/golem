# golem 依赖若干 @deepseek-ai/dsh-* 外部包，其内部 dsh-invariants peer 区间互不
# 重叠（dsh-llm 要 ^0.0.1-rc.1，dsh-typert-protocol 要 ^0.1.1-rc.2）。golem 只
# 用 createUserMessage 与 TypertRemoteService，从不触碰 dsh-invariants，故该
# peer 冲突对运行时无影响。设为 legacy-peer-deps 让 `npm install` 可复现。
legacy-peer-deps=true
