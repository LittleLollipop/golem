# 悬置需求备案（本轮不做）

本文件归档 `requirements.md` 中标记为 **N**（本轮不做）/ **W**（愿望项）的需求。
它们已随 #45–#56 的实现批次一并评审，结论：**本轮不实现**，留作后续独立批次。

> 真源仍以南向 `requirements.md` 的 M/W/N 标注为准；本文件仅作批次级归档，避免后续会话重复立项。

## 清单（共 7 条）

| # | 需求 id | 名称 | 分类 | 不在本轮的理由 |
|---|---------|------|------|----------------|
| 1 | `req_public_api` | 对外 HTTP/API 供第三方接入 | N | 当前以 dsh peer 插件形态内部接入，对第三方开放 API 非本轮 M 目标；需独立的鉴权/限流设计 |
| 2 | `req_metrics_dashboard` | 指标监控面板 | N | 可观察性已由 #55（种子溯源）+ #56（检视 CLI）以日志/CLI 形式覆盖；Web 面板是增量体验，非核心 |
| 3 | `req_multiuser_isolation` | 多用户隔离与权限模型 | N | 维度 I（多假人隔离）已做实例级隔离；跨用户权限模型超出单用户假设，且不得写进核心（见需求注释） |
| 4 | `req_latency_sla` | 延迟硬指标 SLA | N | 无对外 SLA 承诺场景；异步预算(#49)/daemon(#50) 已控制主回合延迟，量化 SLA 是运维级工作 |
| 5 | `req_install_doc_for_others` | 面向他人的安装使用文档 | W | 第二级人评门才需要；本轮聚焦能力实现，文档可最简延后 |
| 6 | `req_cross_platform` | 跨平台（Windows / Linux） | N | 当前 macOS 优先验证；跨平台适配（路径/设备/权限差异）是独立工程批次 |
| 7 | `req_packaging_release` | 打包发布 / 商店上架 | N | 未到发布阶段；发布渠道/签名/上架与核心能力解耦 |

## 备注

- 以上 7 条均已在 `requirements.md` 中带有 N/W 标记，属「设计上已知、本轮主动悬置」，
  而非遗漏。后续若要启动其中任意一条，应作为**新批次**从设计文档（ARCHITECTURE/DESIGN）
  重新评审，而非在现有代码上增量修补。
- 可观察性相关的两条（#2 面板、#4 SLA）其「能力底座」已由 #55/#56 提供数据，
  若未来要做面板，直接消费现有 pre-step 审计日志与 inspect CLI 即可。
