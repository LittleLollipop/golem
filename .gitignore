# Dependencies (build artifacts, never committed)
node_modules/

# Build output
dist/

# Regenerable binary semantic graph (axolotl .axeb) + lock
.semantic-graph/

# Editor / OS noise
.DS_Store
*.log

# Python cache
__pycache__/
*.pyc

# Sidecar runtime default-instance state (regenerated at runtime)
sidecar/config-default.json

# Ambient runtime toggle state (regenerated at runtime, req_ambient_toggle)
ambient-control.json

# L0.5 per-instance knowledge trajectory ledger (regenerated at runtime, req_l05)
.fakeren-knowledge/
